# cutlass examples

## Build & Run

recommend: justfile + cmake + ninja

### Just

Build:

```bash
CUTLASS_PATH=path/to/cutlass VERBOSE=1 just debug
```

Run:

```bash
just run
```

```bash
2.25
```

